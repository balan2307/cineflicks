

export function pageClicked(params,setPage) {

    // let options = {
    //   pathname: `${location.pathname}`,
    //   search: `?${createSearchParams(params)}`,
    // };

    // navigate(options, { replace: true });
    window.location.href = '/trending';

  }